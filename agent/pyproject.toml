[project]
name = "hackspain-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic-ai>=1.0",
    "httpx>=0.28",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", ".."]
